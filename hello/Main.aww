-- Test: basic string concatenation, helper functions, and IO.Stdout.print
import IO.Stdout

main : String -> IOUnit
main input = IO.Stdout.print (addGreeting input)

{- Block comment -}

greeting : String
greeting = "Hello" -- Line comment

addGreeting : String -> String
addGreeting name = greeting ++ ", " ++ name ++ "!"
